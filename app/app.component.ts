import {
    AfterViewInit,
    ElementRef,
    Component
} from '@angular/core';
@Component({
  selector: 'app',
  template: `<main></main>
  <router-outlet></router-outlet>`
})
export class App {
}
